#!/bin/bash
set -euo pipefail
script_dir=$(cd "$(dirname "${BASH_SOURCE[0]}")" &>/dev/null && pwd -P)
cd $script_dir/..

readonly BUILD_DATE=$(date +'%Y-%m-%d %H:%M:%S')
cat > src/build_date.ts <<EOF
// autogenerated by scripts/build_date
export const BUILD_DATE = "$BUILD_DATE";
EOF
